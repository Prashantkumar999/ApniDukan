import React from 'react';
import '../index.css';

const Spinner = () => {
    return (
        <div className='spinner absolute left-[50%] -bottom-[150%]'>
            
        </div>
    );
}

export default Spinner;
